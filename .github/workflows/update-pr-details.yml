name: Update PR details

on:
  workflow_call:

jobs:
  record-pr-details:
    name: Update PR details
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}

      - name: Update PR details
        id: pr-details
        uses: PhraseePhoundry/ga-record-pr-details/update-pr-details@main
